# DocAgent

> 📚 Auto-generated documentation | 2026-04-12

## 📋 Table of Contents

- [Overview](#overview)
- [Architecture](#architecture)
- [Entry Points](#entry-points)
- [Module Relationships](#module-relationships)
- [Design Patterns](#design-patterns)
- [Getting Started](#getting-started)

---

## Overview

This project generates comprehensive documentation for Python projects by analyzing code structure, dependencies, and using language models to provide insights, with the goal of automating the creation of high-quality documentation

## Architecture

The project is structured into several modules, including analysis, generation, core, and parsers, with a main orchestrator that coordinates the workflow, and utilizes design patterns such as the factory pattern for creating parser and LLM provider instances

### Module Dependencies

```mermaid
graph TD
    llm_provider --> llm_models
    config --> config_models
    provider_factory --> llm_provider
    provider_factory --> groq_provider
    provider_factory --> openai_provider
    provider_factory --> config_models
    main --> parsed_file
    main --> python_parser
    main --> scanner
    main --> analyzer
    main --> generator
    base_parser --> parsed_file
    generator --> parsed_file
    generator --> parsed_file
    main_v2 --> parsed_file
    main_v2 --> change_report
    main_v2 --> python_parser
    main_v2 --> scanner
    main_v2 --> change_detector
    main_v2 --> analyzer
    main_v2 --> config
    main_v2 --> dependency_analyzer
    main_v2 --> enhanced_generator
    main_v2 --> generator
    metadata_store --> metadata
    scanner --> scan_result
    enhanced_generator --> generation_result
    enhanced_generator --> parsed_file
    enhanced_generator --> parsed_file
    groq_provider --> llm_provider
    groq_provider --> llm_models
    test_change_detection --> change_detector
    test_change_detection --> parsed_file
    python_parser --> scanner
    python_parser --> parsed_file
    python_parser --> scanner
    dependency_analyzer --> parsed_file
    dependency_analyzer --> dependency_graph
    dependency_analyzer --> scanner
    dependency_analyzer --> python_parser
    parser_factory --> base_parser
    parser_factory --> python_parser
    openai_provider --> llm_provider
    openai_provider --> llm_models
    __init__ --> groq_provider
    __init__ --> openai_provider
    change_detector --> hasher
    change_detector --> metadata_store
    change_detector --> metadata
    change_detector --> change_report
    change_detector --> parsed_file
    metadata --> parsed_file
    mermaid --> parsed_file
    analyzer --> parsed_file
    analyzer --> provider_factory
    analyzer --> llm_provider
    analyzer --> config_models
```

## Entry Points

The following files can be executed directly:

- `main.py`
- `main_v2.py`
- `cli/main.py`

## Module Relationships

Modules connect through imports and function calls, with data flowing from the core scanner and parsers to the analysis modules, and then to the generation modules, which produce the final documentation output

## Design Patterns

- **Factory Pattern**
- **Observer Pattern**
- **Strategy Pattern**

## Getting Started

### Prerequisites

```bash
python >= 3.10
```

### Installation

```bash
# Clone the repository
git clone <repository-url>
cd docagent

# Install dependencies
pip install -r requirements.txt
```

### Usage

```bash
python main.py
```

---

_This documentation was auto-generated by Code Documentation Agent._